export const GameBoard = {
  checkBoard() {
    //Function that would check the board to see if a users input was solvable
    // If it was solvable - return 'solvable'
    //If not solvable - return 'not solvable'
  }
};
